name: Update GitHub Profile README
on:
  push:
  schedule:
    - cron: "*/10 * * * *" 
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Update GitHub Profile README
      uses: theboi/github-update-readme@main
      env:
        GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
      with:
        header: "Hey, I'm Sai Kishore! 👋"
        subhead:   ."
        footer: "**Learn more **"
        ref: "main"
        excludeActivity: '["WatchEvent"]'
        excludeRepo: '["theboi/theboi"]'
